# 原始 API 回應快取 — 可重新抓取，不需要版控（約 8 MB 且每天變動）
data/cache/

# 注意：data/archive/ 刻意「不」忽略。
# 那是每天的判斷快照，是 /archive/ 頁與「跟上期比什麼變了」的唯一資料來源，
# 刪掉或不提交就會永久失去歷史。

# 注意：site/ 也刻意「不」忽略。
# Netlify 直接部署這個目錄，不在雲端重跑 build，所以 FRED 金鑰不需要外流。

__pycache__/
*.py[cod]
.DS_Store
.venv/
