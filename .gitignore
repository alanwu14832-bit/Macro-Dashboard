# 原始 API 回應快取 — 可重新抓取，不需要版控（約 8 MB 且每天變動）
data/cache/

# 注意：data/archive/ 刻意「不」忽略。
# 那是每天的判斷快照，是 /archive/ 頁與「跟上期比什麼變了」的唯一資料來源，
# 刪掉或不提交就會永久失去歷史。

# 注意：site/ 也刻意「不」忽略。
# 建置由 GitHub Actions 跑完 commit 回來，Vercel 只 serve 這個目錄、不重跑 build。
# 產出進版控，Vercel 那邊才不需要 FRED 金鑰，也才不會有第二份判斷歷史。

__pycache__/
*.py[cod]
.DS_Store
.venv/
data/secrets/
